/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "darkfashion.wpengine.com",
      "http://localhost:3001/",
      "w3.org",
      "secure.gravatar.com",
      "localhost",
    ],
  },
};

export default nextConfig;
